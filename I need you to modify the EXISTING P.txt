============================================================
🚨 EMERGENCY STABILITY FIX — DO NOT ADD FEATURES
============================================================

STOP ALL NEW FEATURE DEVELOPMENT.

The PrivacyLens / RECLAIM extension has just suffered a critical regression after the latest risk-engine changes.

This is now a HACKATHON BLOCKER.

The extension was previously working, but now:

1. The RECLAIM Chrome extension popup is stuck on:

   "Detecting..."
   "Checking domain exposure..."

2. It shows:

   Websites: 0
   Exposures: 0
   High Risk: 0

3. The normal website information is not loading.

4. The in-page RECLAIM popup/overlay behavior is also not working correctly.

5. The dashboard/extension synchronization may also be affected.

DO NOT implement any new risk features right now.

DO NOT redesign anything.

DO NOT modify the UI.

DO NOT add more scoring rules.

DO NOT modify unrelated functionality.

OUR ONLY OBJECTIVE:

RESTORE THE LAST KNOWN WORKING EXTENSION.

============================================================
1. CRITICAL RULE
============================================================

The latest risk-engine changes are suspected to have caused the regression.

Before changing anything:

IDENTIFY EXACTLY WHAT BROKE.

Do NOT blindly rewrite the extension.

============================================================
2. FIRST INSPECT GIT HISTORY
============================================================

Inspect the git history and identify:

- Last commit where RECLAIM extension worked correctly
- Current commit
- Files changed between those states

Pay particular attention to:

extension/content.js
extension/background.js
extension/popup/popup.js
extension/popup/popup.html
manifest.json
backend/controllers/eventController.js
backend/controllers/realtimeController.js

Also inspect any newly created:

risk engine files
domain intelligence files
storage helpers
popup trigger files

============================================================
3. IMMEDIATELY CREATE A RECOVERY POINT
============================================================

Before changing anything:

Create a new git branch or checkpoint:

emergency-before-fix

DO NOT destroy the current state.

============================================================
4. FIND THE ACTUAL ERROR
============================================================

Open Chrome:

chrome://extensions/

Find RECLAIM.

Check:

Service Worker → Inspect

Then check the browser console for the affected website.

Look specifically for:

- JavaScript exceptions
- undefined functions
- undefined variables
- failed imports
- syntax errors
- message passing errors
- runtime.lastError
- chrome.runtime.sendMessage errors
- chrome.storage errors
- fetch errors
- SSE/EventSource errors
- JSON parsing errors
- risk-engine exceptions

Report the EXACT error.

============================================================
5. TEST THE BACKGROUND WORKER
============================================================

Verify that background.js successfully starts.

Test:

chrome.runtime.sendMessage

and all existing message handlers.

Verify that messages such as:

GET_CURRENT_STATE
GET_SITE_STATE
GET_EXPOSURE
PAGE_VISIT
FORM_SUBMISSION
RISK_UPDATE

or their actual existing equivalents

are still being handled.

IMPORTANT:

Do not invent new message names.

Use the project's existing message architecture.

============================================================
6. TEST CONTENT SCRIPT
============================================================

Verify that content.js loads on:

https://example.com

and:

http://localhost

and the existing demo website.

Check whether content.js throws an exception during initialization.

VERY IMPORTANT:

If the newly added risk engine executes during content-script startup and throws an exception, it may prevent the rest of content.js from initializing.

This would explain why:

- Website detection stops
- Popup communication stops
- In-page overlay stops

Fix this initialization failure first.

============================================================
7. TEST EXTENSION POPUP
============================================================

Open the RECLAIM browser action.

The popup must successfully progress from:

Detecting...

to:

CURRENT SITE

domain.com

and display:

Risk
Website count
Exposure count

It must NOT remain stuck on Detecting.

============================================================
8. CHECK BACKGROUND → POPUP FLOW
============================================================

Trace:

popup.js
↓
background.js
↓
state/storage/backend
↓
popup.js

Find where the response stops.

For every asynchronous operation verify:

- Promise resolves
- Promise rejects safely
- Errors are caught
- UI receives fallback state

There must NOT be an uncaught promise that leaves the popup permanently in:

Detecting...

============================================================
9. RISK ENGINE MUST NEVER BREAK THE EXTENSION
============================================================

The risk engine is an OPTIONAL ANALYSIS MODULE.

It must NEVER be able to crash:

- content.js
- background.js
- popup.js
- website tracking
- exposure tracking

Wrap risk analysis safely.

Conceptually:

try {
    risk = calculateRisk(...)
} catch (error) {
    console.error("[RECLAIM RISK]", error)

    risk = {
        score: 0,
        level: "LOW",
        reasons: [],
        signals: {}
    }
}

Risk failure must degrade gracefully.

It must NOT stop the rest of the extension.

============================================================
10. TEMPORARILY DISABLE THE NEW RISK ENGINE IF NECESSARY
============================================================

If the new risk engine is confirmed as the cause:

TEMPORARILY DISABLE IT.

Restore the previous working risk behavior.

For example:

website tracking
    ↓
normal state update
    ↓
extension popup

must work even if:

risk calculation

is disabled.

THIS IS MORE IMPORTANT THAN THE NEW RISK FEATURES.

============================================================
11. DO NOT LOSE WEBSITE TRACKING
============================================================

After the fix:

Visit:

site-A.com

Expected:

Websites = +1

Visit:

site-B.com

Expected:

Websites = +1

Reload:

site-A.com

Expected:

No additional website count.

============================================================
12. DO NOT LOSE EXPOSURE TRACKING
============================================================

Existing working behavior must remain.

If an account relationship is detected:

Exposure +1

Repeated login:

Exposure unchanged.

Do not modify this logic unless the regression is directly caused by it.

============================================================
13. RESTORE IN-PAGE RECLAIM OVERLAY
============================================================

The RECLAIM in-page Shadow DOM overlay must load correctly again.

Test:

New website

→ RECLAIM overlay appears according to existing rules.

The overlay must not be blocked because the risk engine fails.

============================================================
14. CHECK MANIFEST
============================================================

Verify manifest.json.

Do NOT accidentally remove:

content_scripts

permissions

host_permissions

background service_worker

action

storage

scripting

or any other permission currently required.

Compare the current manifest against the last known working version.

============================================================
15. CHECK FILE IMPORTS
============================================================

If the latest risk engine introduced something like:

import riskEngine from ...

verify that Chrome Manifest V3 actually supports the current import arrangement.

A missing module/import can kill the content script immediately.

Look for:

Failed to load module script
Cannot use import statement
Module not found
undefined function

etc.

============================================================
16. CHECK DOMAIN INTELLIGENCE
============================================================

If the new domain intelligence module is loaded for EVERY page:

verify that it does not crash when:

domain is undefined
hostname is empty
URL is chrome://
URL is extension://
localhost is used
IP address is used
unusual domain is used

Every parser must safely handle unexpected input.

============================================================
17. CHECK STORAGE
============================================================

Verify:

chrome.storage.local

existing keys.

DO NOT overwrite the complete storage object accidentally.

Especially verify that:

shownPopups

website state

exposure state

risk state

user state

are not being replaced by a new object.

A dangerous pattern would be:

chrome.storage.local.set({
    state: newState
})

when other existing state is expected.

Preserve existing storage structure.

============================================================
18. CHECK SSE
============================================================

The project uses SSE.

Verify:

background.js
↓
EventSource
↓
backend
↓
SSE event
↓
background state
↓
popup

If SSE fails, the extension should STILL load the current local state.

SSE failure must not leave:

Detecting...

forever.

Use a fallback.

============================================================
19. CHECK API FAILURE HANDLING
============================================================

If:

/api/events

or:

/api/realtime

fails,

the extension must not completely stop.

Display the last known local state where possible.

Do not make the popup permanently dependent on a successful network request.

============================================================
20. IMPORTANT — CURRENT SCREENSHOT ANALYSIS
============================================================

The current screenshot shows:

RECLAIM

Score: 100/100

CURRENT SITE

Detecting...

Checking domain exposure...

Digital Exposure Overview:

0 Websites
0 Exposures
0 High Risk

This strongly suggests that the popup UI itself is loading but its DATA INITIALIZATION is failing.

Therefore investigate the data retrieval path FIRST.

Do not redesign the popup.

============================================================
21. IMPORTANT — NETMIRROR AD BLOCK POPUP
============================================================

The large:

"AdBlock Detected"

window visible in the middle of the page is NOT RECLAIM.

It belongs to the website.

Do not modify or attempt to remove it.

Our task is ONLY to restore RECLAIM.

============================================================
22. RECOVERY STRATEGY
============================================================

Use this priority order:

PRIORITY 1
Extension loads.

PRIORITY 2
Popup loads current website.

PRIORITY 3
Website count works.

PRIORITY 4
Exposure count works.

PRIORITY 5
Dashboard synchronization works.

PRIORITY 6
Existing in-page RECLAIM overlay works.

PRIORITY 7
Basic risk score works.

ONLY AFTER ALL OF THE ABOVE WORK:

Re-enable the new advanced risk engine.

============================================================
23. DO NOT CONTINUE FEATURE DEVELOPMENT
============================================================

Until the following are all TRUE:

[✓] Extension opens
[✓] Current domain detected
[✓] Website count works
[✓] Exposure count works
[✓] Popup renders
[✓] In-page overlay renders
[✓] Dashboard works
[✓] Realtime updates work

DO NOT add:

- more risk rules
- more domain categories
- AI detection
- threat intelligence
- new UI
- new popup features

============================================================
24. TEST MATRIX
============================================================

TEST 1

Open:

https://google.com

Expected:

RECLAIM loads.

Current site:

google.com

No "Detecting..." indefinitely.

------------------------------------------------

TEST 2

Open another website.

Expected:

Website count updates.

------------------------------------------------

TEST 3

Open same website again.

Expected:

Website count does not increase.

------------------------------------------------

TEST 4

Open RECLAIM popup manually.

Expected:

Current website information loads immediately.

------------------------------------------------

TEST 5

Disconnect backend temporarily.

Expected:

Extension does not crash.

It should use cached/local state where available.

------------------------------------------------

TEST 6

Reload extension from:

chrome://extensions/

Expected:

Extension works again.

------------------------------------------------

TEST 7

Reload affected website.

Expected:

RECLAIM in-page behavior works.

============================================================
25. ACCEPTANCE CRITERIA
============================================================

The emergency fix is complete ONLY when:

[ ] RECLAIM popup no longer gets stuck at Detecting.

[ ] Current domain loads.

[ ] Website count works.

[ ] Exposure count works.

[ ] Dashboard still works.

[ ] SSE still works.

[ ] Content script loads.

[ ] In-page overlay works.

[ ] Existing popup behavior works.

[ ] Risk engine cannot crash the extension.

[ ] Risk-engine failure gracefully falls back.

[ ] No existing functionality is removed.

[ ] No UI redesign occurs.

[ ] No new features are added.

============================================================
26. FINAL REPORT REQUIRED
============================================================

After fixing:

Tell me:

1. EXACT ROOT CAUSE

2. EXACT FILE THAT CAUSED IT

3. EXACT FUNCTION THAT CAUSED IT

4. EXACT ERROR MESSAGE

5. WHAT WAS CHANGED

6. WHETHER ANYTHING WAS ROLLED BACK

7. HOW YOU VERIFIED THE FIX

8. WHETHER THE NEW RISK ENGINE IS ENABLED OR TEMPORARILY DISABLED

9. MANUAL TEST STEPS

10. GIT COMMIT / CHECKPOINT CREATED

DO NOT say "fixed successfully" without actually testing it.

============================================================
MOST IMPORTANT RULE
============================================================

STABILITY > RISK ENGINE > NEW FEATURES

We are in a hackathon.

A working simple extension is infinitely more valuable right now than a sophisticated risk engine that crashes the entire product.

RESTORE THE LAST KNOWN WORKING STATE FIRST.